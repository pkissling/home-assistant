language: python
python:
  - "3.6"
install:
  - pip3 install homeassistant
script:
  - hass -c . --script check_config
before_install:
  - mv dummy_files/secrets_dummy.yaml secrets.yaml
  - mv dummy_files/fullchain_dummy.pem fullchain.pem
  - mv dummy_files/privkey_dummy.pem privkey.pem