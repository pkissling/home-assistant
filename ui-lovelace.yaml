---

background: var(--background-image)

custom_header:
  background: 'rgba(155, 155, 155, 0.25)'
  compact_mode: false
  button_text:
    options: '{{ hours24 }}:{{ minutesLZ }}'

views:
  !include_dir_merge_list lovelace
